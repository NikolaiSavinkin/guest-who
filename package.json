{
  "name": "guest_who",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf back/dist front/dist shared/dist",
    "build:all": "yarn run build:shared && yarn run build:backend && yarn run build:frontend",
    "build:shared": "yarn --cwd shared install && yarn --cwd shared run build",
    "build:backend": "yarn --cwd back install && yarn --cwd back run build",
    "build:frontend": "yarn --cwd front install && yarn --cwd front run build",
    "start:backend": "yarn --cwd back run start",
    "start:frontend": "yarn --cwd front run start"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.14.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3",
    "vite": "^6.2.0"
  }
}
